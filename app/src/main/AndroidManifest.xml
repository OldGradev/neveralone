<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.utente.neveralone">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- prima activity -->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- seconda activity -->
        <activity android:name=".AssistenzaActivity" />

        <!-- terza activity -->
        <activity android:name=".VolontarioActivity" />

        <!-- quarta activity -->
        <activity android:name=".MessaggioActivity" />

        <!-- quinta activity -->
        <activity android:name=".DettagliActivity" />

        <!-- sesta activity -->
        <activity android:name=".RegistrazioneActivity"/>
        <!-- ATTENTION: This was auto-generated to add Google Play services to your project for
             App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

    </application>

</manifest>
